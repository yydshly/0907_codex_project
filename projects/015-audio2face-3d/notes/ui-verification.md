# 浏览器验收记录

日期：2026-09-08。通过 Codex 内置浏览器操作实际本地页面，未用静态截图替代交互验证。

| 项目 | 结果 |
| --- | --- |
| 页面初次载入 | 官方人物显示，模型状态为“真实模型输出”，GPU 服务状态为“本机已就绪” |
| 播放 | 点击播放后切换暂停状态；时间轴推进至 9 秒，结束后恢复播放按钮 |
| 时间轴 | 拖动至 0.339 / 0.45，音频时间更新为约 3 / 4 秒，面部姿态更新 |
| 表情 | 平静 / 喜悦按钮切换有效，读取相应真实推理结果 |
| 视角 | 正面 / 侧面切换有效 |
| 网格 | 线框开关的 aria-pressed 状态更新，三维网格模式可切换 |
| 速度 | 选择 0.5× 成功，原音频与动画使用同一音频时间轴 |
| 上传 | 通过文件选择器上传 9.62 秒中文 WAV，POST 返回 200，显示“已生成”、289 帧与自定义文件名 |
| 上传后切换表情 | 对同一录音再次请求 joy 并生成对应数据 |
| 桌面 | 人物、样例、表情按钮、播放条正常显示，较矮视口缩短舞台以保留播放操作 |
| 手机 | 390×844 实测，单列排版、人物与播放条可见；恢复默认视口后继续展示 |
| 浏览器日志 | 检查时无 error / warn 日志 |
| 静态子路径 | `/demos/015-audio2face-3d/` 加载人物和表情结果成功；上传显示“需本机服务”并禁用，日志无 error / warn |

数值验证与生成耗时见 `verification.json` 和 `inference-results.json`。未开展用户感知评分、真实真人语音大规模评估或生产负载测试。
