# 中文研究导览

`index.html` 为独立静态网页，HTML、CSS、JavaScript 内联，无安装或构建依赖，可直接在浏览器打开。通过本地静态服务器访问时，入口为 `/demos/013-fde-interview-fieldbook/`（服务根目录设为 `docs`）。

发布文件为 `docs/demos/013-fde-interview-fieldbook/index.html`，与此处源码保持一致。更新时复制源文件至发布目录。发布地址：[案例与技术导览](https://yydshly.github.io/0907_codex_project/demos/013-fde-interview-fieldbook/)。随 main 分支的 docs 目录发布，部署状态以 GitHub Pages 构建结果为准。

页面是原创研究整理，资料引用均链接至上游研究提交；FIELD 示例为解释用途，不是运行中的业务系统。案例分类及四类阅读建议由本页整理，正式训练日程以上游为准。

页面现包含十套案例的本页精读与自测、六组技术指南、知识助手六步实践示例，以及我们的仓库路径和笔记下载。案例不是运行演示，其效果尚未在我们的生产环境验证。

`../notes/practice-guide.md` 与 `../notes/project-checklist.md` 是笔记主文件。修改笔记后，将两份文件复制到此目录及 `docs/demos/013-fde-interview-fieldbook/`，保证本地和发布页下载一致。

## 本地验证

2026-09-07 使用 Chromium 检查：五个 FIELD 步骤、四个案例筛选（10/3/3/4）、四类阅读路径均正常；1440、768、390 像素宽度无横向溢出；页内锚点有效，无浏览器脚本错误。已查看桌面与手机整页截图，源文件与发布文件摘要一致。以上为本页验证，不代表上游训练效果验证。

同日内容扩充后再次验证：十套案例切换、参考判断展开、每案复盘来源、筛选后的选中状态、六组技术卡、六步实践示例与两份下载文件均正常。原有 FIELD 和阅读路径交互继续通过，三档屏幕无横向溢出，页面与笔记副本一致。

## 网页检查与修复（2026-09-07）

- 修复尚未发布的 GitHub 子项目入口返回 404：保留完整库内路径，使用页内资料入口和已存在的研究库目录。
- 来源链接写入静态 HTML，禁用 JavaScript 仍可访问；默认退款案例预先显示，全部内容可下载。
- 案例采用 `?case=案例ID#case-reader` 固定链接，支持收藏、刷新恢复和浏览器后退；增加当前分类内前后切换与边界禁用。
- 顶部导航改为不透明背景，标识当前阅读区；章节跳转即时定位，首屏按钮可换行，补充打印样式。
- 逐一通过十个案例、筛选、历史、下载、键盘跳过导航、FIELD 与阅读路径回归；320/390/620/768/1024/1440 宽度无横向溢出，手机章节标题不被导航遮挡。
- 禁用 JavaScript 及带参数首次打开均通过；无重复元素 ID。通过上游固定提交的目录清单核对 20 个案例来源文件及其他资料来源；两个研究库入口实际返回 HTTP 200。

这些检查针对静态网页的内容呈现与操作，不代表已经验证上游方法的生产效果。发布副本与源文件保持一致。

## 外部介绍图

`../assets/` 保存原创 SVG 与 PNG 引导图，网页与站点副本放在各自的 `assets/`。更新时同步三处资源，并检查网页、子项目 README 和根索引的图片链接。架构图描述资料与使用方法，不表示仓库提供业务运行服务。
